const express=require('express');const router=express.Router();const {protect,authorize}=require('../middlewares/auth.middleware');const c=require('../controllers/announcement.controller');router.get('/active',protect,c.getActiveAnnouncement);router.get('/',protect,authorize('admin','superadmin'),c.getAnnouncementsAdmin);router.post('/',protect,authorize('admin','superadmin'),c.createAnnouncement);router.put('/:id',protect,authorize('admin','superadmin'),c.updateAnnouncement);router.delete('/:id',protect,authorize('admin','superadmin'),c.deleteAnnouncement);module.exports=router;
